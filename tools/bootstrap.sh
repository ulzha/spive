# Usage:
#   $ bootstrap --thread-runner=localhost (action container) --stream=spive-prod-bootstrap.jsonl
#   $ bootstrap --thread-runner=some.spivethreadrunner.e.europe.company.net --stream=spive-test-bootstrap.jsonl (--daemon)
#   $ bootstrap --thread-runner=localhost --stream=spive-dev-allthesetupthatwewant.jsonl (--daemon)
