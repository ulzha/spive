package io.ulzha.spive.example.clicctracc.app.events;

import lombok.Data;

// not sure where this comes from - also generated? Should reside in lib/? Or Artifactory always?
@Data
public class Clicc {
  // no partition keys
  // no fields whatsoever
}
